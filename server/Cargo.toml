[package]
name = "health_server"
version = "0.1.0"
edition = "2024"

[dependencies]
health_shared = { path = "../shared" }
bevy.workspace = true
lightyear = { workspace = true, features = ["server"] }

[lib]